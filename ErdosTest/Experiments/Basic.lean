import Mathlib

/-!
# 实验（Experiments）

这里存放针对某个 Erdős 开放问题的**计算实验**。

用途：
- 用 `#eval` / `decide` / `norm_num` 做小规模枚举、搜索反例、验证猜想的有限情形。
- 记录数据与观察，帮助提出或反驳猜想。

⚠️ 约定：实验产出是**实验观察（experimental observations）**，**不是**证明。
只有当某条观察被完整形式化证明后，才可升格为定理并移入 `ErdosTest/Theorems/`。

分类（详见 `ErdosTest/Notes/README.md`）：
1. 已知数学（known mathematics）
2. 实验观察（experimental observations）
3. 猜想（conjectures）
4. 形式化验证结果（formally verified results）
-/

namespace ErdosTest.Experiments

end ErdosTest.Experiments
