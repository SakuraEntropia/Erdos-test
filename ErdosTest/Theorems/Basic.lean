import Mathlib

/-!
# 定理（Theorems）

这里存放**形式化验证**的定理：在 Lean 中已完整证明、**无 `sorry`** 的结果。

已收录：
- `ErdosTest.Szekeres` —— Erdős–Szekeres 单调子序列定理（弱版本）。

约定：
- 每个结果都必须通过 `lake build` 验证。
- 禁止 `sorry`、`admit` 或任何未完成的证明。
- 任何“开放问题已被解决”的说法，必须伴随一个完整的 Lean 证明。
-/

namespace ErdosTest.Theorems

end ErdosTest.Theorems
